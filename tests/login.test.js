function sum(x,y){
    return x+y;
}
test('adds 1 + 2 to equal 3', () => {
    expect(sum(1, 2)).toBe(3);
  });

test('should add referee successfully', () => {
    //implement
  });

test('referee_id already exist,', () => {
    //implement
  });

test('invalid details', () => {
    //implement
  });

test('unauthorized', () => {
    //implement
  });

